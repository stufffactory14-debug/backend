const arr = [1, 2, 3, 4];

let total = 0;

for (let i = 0; i < arr.length; i++) {
    total += arr[i];
}

console.log(total);


const arr = [1, 2, 3, 4];

const total = arr.reduce((sum, current) => sum + current, 0);

console.log(total);
